# 2026-07-31 开发进度

## 今日任务

`S1-RENDER-001 微信 HTML Renderer 核心`

## 完成内容

- 实现 `@wechat-layout/wechat-renderer` 服务端渲染包，从通过校验的 Document Schema V1
  JSON 生成微信正文 HTML，不依赖编辑器 DOM；
- 建立可冻结的 Node Renderer 和 Component Renderer 注册表，拒绝重复注册及冻结后修改；
- 覆盖段落、三级标题、引用、无序列表、有序列表、图片、分割线、语义卡片、品牌页脚和
  SVG 互动占位共 11 类 Block Renderer；
- 接入 Token Engine，按标准、安全和静态模式解析系统、主题、品牌、组件与节点局部样式；
- 接入 Component Registry，按文档固化的 `componentId@version` 精确读取 Manifest，
  再调用声明的内置 `wechatRendererKey`；
- 建立安全 HTML AST，不开放 Raw HTML；集中执行标签、属性、URL 和 CSS 白名单序列化；
- 将所有样式内联，确定性排序属性和声明，并转义正文、属性及组件内容；
- 图片和链接只接受不含凭据、不指向本机或私网的公网 HTTPS URL；
- 缺失或非法图片生成可读占位，缺失或停用组件使用保留原文的安全结构；
- 标准模式保留受控视觉样式，安全模式移除复杂背景、阴影和风险定位，静态模式继承安全
  规则并转换互动内容；
- SVG 安全引擎尚未进入本阶段，因此所有模式暂使用文档中的静态备用图并返回明确提示；
- 输出 Renderer `1.0.0` 版本、HTML SHA-256、原文哈希、资源 ID、组件精确版本和文档
  Schema 版本 Manifest；
- 增加无效文档、原文哈希不匹配、Token 错误、嵌套降级和缺失 Renderer 的结构化失败；
- 新增包级 README，并更新根 README 的开发状态和下一任务。

## 测试结果

- `pnpm format:check`：通过；
- `pnpm lint`：通过；
- `pnpm typecheck`：23/23 个任务通过；
- `pnpm test`：23/23 个 Node 任务与 Python 测试通过；
- Renderer 包：2 个测试文件、9 个测试通过；
- HTML 快照：通过，覆盖确定性全内联输出；
- 安全测试：通过，覆盖危险标签、文本转义、非法 CSS、HTTP、凭据、本机和私网 URL；
- 模式对比：标准输出保留受控背景、阴影和相对定位，安全/静态输出完成降级；
- 组件测试：精确使用 `componentId@version` 和 Manifest Renderer Key；
- `pnpm build`：16/16 个 Node 构建任务与 Python 编译检查通过；
- `dist/index.js` 真实 ESM 导入与最小文档渲染：通过；
- `pnpm docker:smoke`：10/10 步通过，现有应用、数据库、Redis、MinIO 和持久化均正常。

## 技术决策

- Renderer 只消费权威文档 JSON；编辑器 DOM、Node View、Class 和交互控件不进入输出路径；
- HTML 只能由类型化安全 AST 构造，组件 Renderer 也不能返回任意 HTML 字符串；
- `standard`、`wechat_safe`、`static` 使用同一节点语义，仅由 Token 和样式策略执行可比较
  降级；
- 安全模式禁止 `background` 简写、背景图和阴影，只允许 `position: static`；标准模式也
  禁止 `fixed` 与 `sticky`；
- URL 清洗不负责网络请求，仅建立最终输出边界；资源获取、签名和公开地址生成继续由资源
  服务负责；
- 有序列表保留原编号时关闭浏览器原生编号，避免微信粘贴后出现重复编号；
- SVG 动态执行不提前实现，当前强制静态备用图，等待 `S2-SVG-001` 的协议与安全引擎；
- 兼容评分、问题规则版本和自动修复不并入本任务，留给紧随其后的 `S1-COMPAT-001`。

## 发现问题

- 当前交互 shell 默认 Node.js 20，工作区运行时提供 Node.js 24，但配套 pnpm 为 11；
  验证统一使用 Node.js 24 执行项目冻结的 pnpm 10.33.0；
- 离线依赖安装因本机 Store 缺少 NestJS Tarball 中断，授权使用现有网络安装流程后依赖与
  workspace 锁文件恢复完成；
- pnpm 仍报告 Vite 间接 WASM 绑定的既有可选 Peer 警告，不影响安装、测试或构建。

## 阶段结论

`S1-RENDER-001` 的任务和验收项已完成。按开发总指令，下一任务是
`S1-COMPAT-001 兼容规则基础`，未经用户确认不自动开始。

---

## 今日任务（二）

`S1-COMPAT-001 兼容规则基础`

## 完成内容

- 在 `@wechat-layout/wechat-renderer` 内建立兼容规则版本 `1.0.0`，规则定义和报告均深冻结；
- 将兼容规则版本写入 Renderer Manifest，使 HTML 输出、Renderer 和规则包可追溯；
- 实现 Document JSON 与最终 HTML 双层兼容检查，复用 Renderer 的标签、CSS 和 URL 安全
  边界；
- 使用 Parse5 扫描最终 HTML AST，不以正则替代 HTML 结构解析；
- 覆盖危险/未知标签、事件/未知/URL 属性、CSS 属性和值、高风险定位、链接、图片、嵌套、
  组件和 SVG 静态降级规则；
- 检测空图片、私网或非 HTTPS 图片、超宽图片、缺少 `max-width:100%` 和替代文字；
- 检测 Document JSON 中的 HTTP、Mailto、私网及非法链接，并定位原始 Block ID；
- 检测资源清单中的缺失图片和 SVG 静态备用图，并定位对应 Block ID；
- 将 Renderer 缺失组件、Token 回退、安全策略清理、嵌套展开和 SVG 降级映射为兼容问题；
- 为每项问题生成确定性 Issue ID、规则 ID、严重度、分类、来源、路径、Block ID 和修复动作；
- 实现 0—100 评分、`passed / warning / failed` 状态和严重/警告/建议摘要；
- 严重问题统一返回 `canCopy: false`，为后续一键复制建立强制前置门禁；
- 实现 Document 修复预览：移除非法链接 Mark、保留链接文字、约束图片宽度到 100%；
- 实现 HTML 修复预览：删除危险标签和事件属性、清理非法样式/链接、约束图片宽度、展开
  未验证容器并保留正文；
- 所有修复均基于结构化克隆并返回深冻结预览，不直接修改权威文档；
- 更新包级 README、根 README 和下一任务说明。

## 测试结果

- `pnpm format:check`：通过；
- `pnpm lint`：通过；
- `pnpm typecheck`：23/23 个任务通过；
- `pnpm test`：23/23 个 Node 任务与 Python 测试通过；
- Renderer / Compatibility 包：3 个测试文件、15 个测试通过；
- 文档检查：覆盖非法链接、缺失图片、SVG 备用图和组件 Renderer 缺失的 Block ID 定位；
- HTML 检查：覆盖脚本、未知标签、事件属性、URL 属性、非法 CSS、超宽/空图片和危险链接；
- 自动修复：验证原文档不变、链接文字保留、图片宽度收敛和修复后重新检查；
- 安全模式：验证复杂背景、阴影和非 `static` 定位被识别；
- `pnpm build`：16/16 个 Node 构建任务与 Python 编译检查通过；
- `dist/index.js` 真实 ESM 导入与兼容检查：通过；
- `pnpm docker:smoke`：10/10 步通过。

## 技术决策

- 兼容核心保留在 Renderer 包内，共享同一标签、CSS 和 URL 策略，避免检查规则与真实输出
  漂移；
- 本任务只实现可复用规则引擎，不提前创建兼容报告数据库表、API Job、抽屉 UI 或忽略记录；
- Document 检查即使遇到 Schema 非法输入也继续做安全扫描，从而同时返回结构错误和可定位的
  具体链接/图片问题；
- HTML 输出使用 AST 扫描，危险标签删除子树，未知普通容器只展开并保留可见正文；
- 自动修复只处理不会改写正文语义的安全项；缺失图片和 SVG 资源仍是严重问题，必须补齐资源；
- Issue ID 由规则、来源、位置、Block 和详情确定性生成，便于后续持久化、忽略和修复对账；
- 评分采用版本化规则罚分并限制在 0—100，任何严重问题都独立于分数阻止正式复制。

## 发现问题

- 离线安装再次因本机 pnpm Store 缺少 API 测试依赖 Tarball 中断，授权联网后按锁文件恢复；
- pnpm 继续报告 Vite 间接 WASM 绑定的既有可选 Peer 警告，不影响兼容引擎；
- 当前 HTML 规则基于设计文件与自动化测试，微信后台、iPhone 和 Android 实机矩阵留给
  `S1-TEST-001` 和复制任务人工验收。

## 阶段结论（二）

`S1-COMPAT-001` 的代码和自动化验收项已完成。下一任务是
`S1-COPY-001 一键复制`，未经用户确认不自动开始。

---

## 今日任务（三）

`S1-COPY-001 一键复制`

## 完成内容

- 新增 `content.render_outputs` 和 `content.copy_records`，保存正式微信输出、兼容报告、
  快照绑定、双格式内容、浏览器结果和失败原因；
- 新增迁移 `0003_talented_stephen_strange.sql`，同步更新 Schema 结构、索引、外键和数据库
  验收清单；
- 实现 `POST /articles/:articleId/render-wechat`，从指定 `documentVersion` 的服务端权威文档
  JSON 生成正式 Renderer 输出，不读取编辑器 DOM；
- 正式输出在同一事务中创建 `before_copy` 不可变快照，固化 Renderer `1.0.0`、兼容规则
  `1.0.0`、主题/品牌版本、资源清单、输出哈希和兼容评分；
- 接入兼容引擎强制门禁：严重问题仍保存可定位报告，但输出标为 `blocked / failed`，不发放
  Copy Payload；
- 实现渲染输出读取和 15 分钟短时 Copy Payload 接口，同时返回 `html` 与 `plainText`；
- 为文档版本冲突、输出过期、兼容阻止、所有权隔离、CSRF 和无效 UUID 提供稳定错误；
- 实现浏览器复制结果回写，保存成功/失败、受控浏览器信息和失败原因，并更新文章状态历史
  与审计日志；
- 编辑器页面新增一键复制面板，支持标准、安全、静态三种输出模式；
- 生成正式内容和写入剪贴板分为两次用户点击，避免异步生成后丢失用户激活；
- Clipboard API 使用单个 `ClipboardItem` 同时写入 `text/html` 和 `text/plain`；
- 运行时检测安全上下文、用户激活、Clipboard API、HTML MIME 支持和浏览器权限；
- 失败时显示只包含服务端安全输出的受控 `contenteditable` 手动复制区，支持全选后由用户
  按 Command+C / Ctrl+C；
- 成功文案只声明“已写入剪贴板”，明确要求到微信公众号后台补充标题、封面、粘贴和预览，
  不声称已经发布；
- 更新 Docker 烟测，覆盖复制 OpenAPI、正式输出、Payload、复制记录、快照和数据库持久化。
- Web 生产构建切换到 Next.js 官方 Webpack 模式，规避 Next.js 16.2.12 Turbopack 在当前
  中文工作区路径下的 UTF-8 标识符崩溃；开发模式不受影响。

## 测试结果

- `pnpm format:check`：通过；
- `pnpm lint`：通过；
- 强制全仓类型检查：24/24 个任务通过；
- 强制全仓 Node 测试：24/24 个任务通过，Python 测试通过；
- API：14 个测试文件、58 个测试通过；
- Web：16 个测试文件、54 个测试通过；
- 新增 Clipboard 测试：覆盖 HTML/纯文本双 MIME、安全上下文、用户激活、能力缺失和权限拒绝；
- 新增复制面板测试：覆盖第二次用户点击、成功提示、手动兜底和 Block ID 严重问题定位；
- 新增 API 合同测试：4 项通过，覆盖正式输出、Payload、结果回写、CSRF、所有权、版本冲突、
  兼容阻止和 OpenAPI；
- 数据库迁移往返测试：15 张表、55 个索引、31 个外键、4 个迁移和 1 个快照不可变触发器
  验收通过；
- 空库迁移、种子幂等、测试库回滚和重新迁移：通过；
- `pnpm build`：16/16 个 Node 构建任务与 Python 编译检查通过；
- `pnpm docker:smoke`：10/10 步通过，正式输出、双格式 Payload、复制记录、快照、审计、
  PostgreSQL、Redis、MinIO 和服务重启均正常；
- 本地浏览器真实验收通过：未保存时生成按钮禁用，保存后才解锁；生成后才允许第二次点击
  写入；同一 Clipboard Item 同时包含 `text/html` 和 `text/plain`，两种格式均含权威正文；
  成功提示明确不代表已发布。

## 技术决策

- `S1-JOB-001` 尚未实施，本阶段采用同步正式渲染事务；API 与持久化边界已经独立，后续可将
  生成步骤迁移到 BullMQ，而不改变 Copy Payload 和浏览器协议；
- Payload 只从已持久化且 `canCopy=true` 的正式输出发放，浏览器不能上传任意 HTML 伪造
  正式结果；
- 私有资源在生成时换取与 Payload 同期的短签名 URL；非 HTTPS、私网、缺失或签名失败的资源
  会被 Renderer/兼容引擎阻止；
- Clipboard 成功仅表示写入系统剪贴板，不能证明微信公众号后台保留全部样式，更不能代表
  已保存草稿或发布；
- 用户代理信息限制键名、数量和长度后再落库，日志与 API 响应不记录 Cookie、Token 或
  剪贴板正文；
- 浏览器不支持富文本或拒绝权限时不调用不可靠的自动 `execCommand` 冒充成功，而是进入
  可见、可选择、可操作的手动复制流程。

## 发现问题与人工验收

- 本地 `localhost / 127.0.0.1` 可作为浏览器开发安全上下文，生产环境仍必须使用 HTTPS；
- Next.js 16.2.12 Turbopack 在项目当前中文绝对路径下会因 UTF-8 字节边界发生内部崩溃；
  生产构建改用官方 `--webpack` 选项后 16/16 个任务通过；
- 自动化已覆盖 Chrome、Safari 和 Edge 共用的标准 API 与能力检测分支，但当前机器未执行
  Safari / Edge 真实浏览器矩阵；
- “粘贴微信公众号后台、保存草稿、iPhone / Android 微信预览”需要可登录公众号和真实设备，
  本次无法自动执行，保留到 `S1-TEST-001` 人工验收矩阵；
- pnpm 继续报告 Vite 间接 WASM 绑定的既有可选 Peer 警告，不影响复制实现。

## 阶段结论（三）

`S1-COPY-001` 的实现、数据库迁移和自动化验收项已完成。按第四轮串行顺序，下一任务是
`S1-WEB-002 V0.1 页面`，未经用户确认不自动开始。
