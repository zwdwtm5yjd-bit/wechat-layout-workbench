# 2026-07-29 开发进度

## 今日任务

`S0-ARCH-001 Monorepo 初始化`

## 完成内容

- 将 00—16 号开发文件归档到 `docs/`；
- 初始化 Git 仓库和 `feature/S0-ARCH-001-monorepo` 分支；
- 建立 pnpm Workspace 与 Turborepo；
- 建立 Web、API、Worker、Scheduler 四个应用空骨架；
- 建立 Python DOCX Worker 服务占位；
- 建立 11 个共享包边界；
- 配置 TypeScript、ESLint、Prettier、Vitest；
- 配置非敏感环境变量 Schema 和 `.env.example`；
- 验证四个应用可同时启动。

## 测试结果

- `pnpm install --frozen-lockfile --prefer-offline`：通过；
- `pnpm format:check`：通过；
- `pnpm lint`：通过；
- `pnpm typecheck`：16/16 个任务通过；
- `pnpm test`：16/16 个 Node 任务通过，3 个 Vitest 测试与 1 个 Python 测试通过；
- `pnpm build`：15/15 个 Node 构建任务与 Python 编译检查通过；
- `pnpm dev`：Next.js、NestJS、Worker、Scheduler 均成功启动。

## 技术决策

- Node.js 锁定 `24.13.0`，pnpm 锁定 `10.33.0`；
- Next.js 锁定 `16.2.12`，NestJS 锁定 `11.1.28`；
- TypeScript 使用 `5.9.3`，优先保证当前 NestJS、Next.js 和 ESLint 工具链兼容；
- 共享包当前仅建立边界，未提前实现文档 Schema、主题、组件、Renderer 或微信连接能力；
- `packages/config` 只提供 S0 所需的公开运行参数基线，完整密钥和配置管理留待 `S0-ARCH-003`。

## 发现问题

- pnpm 对 Vitest 间接依赖的 WASM 备用绑定报告可选 peer 警告；当前 macOS ARM64 使用原生绑定，安装、测试和构建均通过。

## 阶段结论

`S0-ARCH-001` 已完成并提交；用户确认后进入 `S0-ARCH-002`。

---

## 今日任务（二）

`S0-ARCH-002 Docker 开发环境`

## 完成内容

- 创建基础 Compose 与开发端口覆盖文件；
- 固定 PostgreSQL 18.4、Redis 8.2.8、MinIO、Mailpit 和 Node.js 镜像版本；
- 容器化 Web、API、Worker、Scheduler 四个应用进程；
- 为全部常驻服务配置健康检查；
- 配置 PostgreSQL、Redis、MinIO、Mailpit 命名数据卷；
- 配置 Redis AOF 持久化和 PostgreSQL 18 数据卷挂载；
- 创建 MinIO 初始化任务，自动建桶并上传 `healthcheck.txt`；
- 创建本地 Docker 环境文件生成器，随机密钥权限为 `600` 且不进入 Git；
- 创建一键启动、停止、日志、状态、Compose 校验和完整烟测命令；
- 将所有宿主机端口限制在 `127.0.0.1`；
- 完成 Docker CLI、Compose 与 Colima 本机开发运行时安装，服务保持运行。

## 测试结果

- `pnpm docker:config`：Compose 合并配置校验通过；
- `pnpm docker:dev`：一条命令完成单次 Node 镜像构建并启动全部服务；
- `pnpm docker:smoke`：PostgreSQL、Redis、MinIO、Mailpit、Web、API、Worker、Scheduler 健康检查通过；
- PostgreSQL：连接通过，服务重启后持久化探针保留并清理；
- Redis：认证连接通过，服务重启后 AOF 探针保留并清理；
- MinIO：初始化任务完成建桶与测试文件上传，服务重启后对象仍可读取；
- API：容器内 TCP 健康检查和宿主机 `3001` 端口检查通过；
- Web、MinIO、Mailpit：宿主机 HTTP 端点检查通过；
- `pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`：全部通过。

## 技术决策

- S0-ARCH-002 只使用 TCP 检查 API 进程；`/health/live` 与 `/health/ready` 留给 `S0-API-001`；
- 应用镜像只在 API 服务声明构建，其他三个应用复用同一镜像，避免重复安装依赖；
- MinIO 由一次性初始化容器完成建桶和上传验收，不在应用代码中提前实现存储业务；
- PostgreSQL、Redis、MinIO、Mailpit 及应用镜像均使用固定版本，不使用 `latest`；
- `.env.docker` 由本机生成，不提交密码；`docker:config` 只做静默校验，避免在终端展开敏感值；
- 未提前创建测试或生产 Compose，保持本任务只覆盖开发环境。

## 发现问题

- Docker Desktop 的 Homebrew 安装需要本机管理员密码，安装过程已自动回滚；改用 Docker CLI、Compose 与 Colima 完成验收；
- Colima 首次启动遇到 Ubuntu 镜像 DNS 符号链接失效，修复为持久解析文件并正常完成二次启动；
- Compose 检测到本机未安装 Buildx，已自动使用 classic builder，构建与运行结果不受影响；
- 首轮烟测发现 MinIO 服务容器的匿名别名不能用于验收，已改为独立认证客户端并复验通过。

## 风险

- MinIO 社区 Docker 镜像固定在最后一个可用发布版本；进入部署任务前需要重新评估其维护状态和生产替代方案；
- 当前电脑使用 Colima，团队成员也可使用 Docker Desktop；项目脚本同时兼容 `docker compose` 和独立的 `docker-compose`。

## 下一任务

建议进入 `S0-ARCH-003 配置管理`，但未经确认不自动开始。
