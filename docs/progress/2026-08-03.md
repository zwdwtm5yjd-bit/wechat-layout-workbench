# 2026-08-03 开发记录

## 今日任务

`S2-OPS-002 日志监控`

## 完成内容

- 新增带独立 Bearer Token 的 `/internal/metrics`，并在 Nginx 公网入口精确返回 404；
- 增加 API 请求次数、错误码、耗时直方图、进程 CPU/内存、队列状态、最老等待任务、Worker
  心跳、指标采集状态和 Loki 投递状态；动态 UUID / 数字路径统一为 `:id`，避免高基数；
- 结构化日志继续写 stdout，并以有界批次异步推送 Loki；失败或队列溢出会进入指标；
- API 接入 OpenTelemetry Node 自动插桩，经 OTLP Collector 写入 Tempo；
- 生产 Compose 新增 Prometheus、Alertmanager、Grafana、Loki、Tempo、OpenTelemetry Collector
  和 Node Exporter，镜像固定版本与 digest，监控网络禁止外部路由且不挂载 Docker Socket；
- Grafana 预置 Prometheus / Loki / Tempo 数据源和运行总览；Grafana 只绑定宿主回环端口；
- 基础告警覆盖 API、保存、Worker、队列、主机资源、采集器和日志投递；告警 URL、指标 Token、
  Grafana 密码使用 Compose Secret 文件；
- 部署脚本加入监控启动顺序、内部健康检查和显式 `docker:prod:alert-test` 告警链路测试；
- 新增监控运行手册，明确 SSH 隧道、保留周期、安全边界、告警验收和真实环境门槛。

## 当前验证结果

- Config：4 个文件、13 个测试通过；
- API：24 个文件、86 个测试通过，其中 Observability 5 个文件、10 个测试；
- Infrastructure：5 个文件、23 个测试通过；
- 全工作区 26/26 个测试任务、26/26 个类型任务、17/17 个构建任务通过；
- 全仓格式检查与 Lint、Python 测试通过；
- Docker 监控镜像已拉取并记录官方仓库 digest；
- 生产 Compose 模板与安全约束检查通过；Prometheus、Alertmanager、Loki、Tempo、
  OpenTelemetry Collector 均通过对应制品的配置校验；
- 隔离 Grafana 容器通过健康检查，三个数据源与预置 Dashboard 均完成 Provisioning。

## 阶段结论

S2-OPS-002 的仓库制品与本地容器制品验收已完成。真实上线验收仍需要：生产等价环境完整启动、
Grafana / Loki / Tempo 人工查询、持久卷重启验证，以及真实 HTTPS 告警中继收到 firing /
resolved。上述门槛关闭前，不声明“生产监控已经生效”。
