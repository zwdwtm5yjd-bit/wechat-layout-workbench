# V0.1 发布检查清单

> 基线任务：`S1-TEST-001`  
> 更新日期：2026-08-03
> 判定规则：所有 P0 / P1 门槛通过后才允许打 V0.1 标签。

## 自动化质量门禁

上一远端基线提交 `4fdcd35` 的 GitHub Actions Run `30778729423` 已全部通过。当前工作区新增了
文档资源事务绑定、快照冻结、历史回填和含图验收种子；下表只记录本轮已经实际执行的证据，
未重跑的门禁不得沿用旧结论。

| 检查项 | 命令或证据 | 当前状态 |
| --- | --- | --- |
| 格式 | `pnpm format:check` | **通过** |
| ESLint | `pnpm lint` | **通过** |
| TypeScript | `pnpm typecheck` | **通过**；26/26 工作区任务及 Infrastructure `tsc` |
| 单元与 API HTTP 测试 | `pnpm test` | **通过**；26/26 工作区任务、API 76/76、Web 65/65、seed support 3/3、Python 1/1 |
| Testcontainers | `pnpm test:integration` | **待重跑**；核心资源事务 5/5 已通过，新增旧库回填与 trash 竞态尚待容器复验 |
| Chromium / WebKit E2E | `pnpm test:e2e` | **待本轮重跑** |
| 生产构建 | `pnpm build` | **通过**；17/17 工作区构建及 Python compileall |
| 数据库迁移 | `pnpm db:test:migrations` | **待本轮重跑** |
| Docker 全链路烟测 | `pnpm docker:smoke` | **待本轮重跑**；烟测已改为通过文档 API 自动绑定/解绑图片 |
| 远端 CI | `.github/workflows/v0.1-ci.yml` | 上一基线全绿；**当前提交待推送后确认** |

## 人工验收数据准备

脚本始终通过正常登录、资源、文章、文档、主题、Renderer 和 Copy Payload API 工作，不直连
数据库，不修改 Owner 密码，也不覆盖旧文章。正常登录会更新登录时间、会话和审计记录；请
使用正确的 active Owner 凭据，避免错误尝试累计。失败不会自动删除已经完成的资源或文章，
可通过输出的 `runLabel` 识别本次结果。

### Safari

```bash
export ACCEPTANCE_OWNER_EMAIL="<现有 active Owner 邮箱>"
read -s "ACCEPTANCE_OWNER_PASSWORD?Owner password: "
export ACCEPTANCE_OWNER_PASSWORD
ACCEPTANCE_SCOPE=safari pnpm acceptance:seed
unset ACCEPTANCE_OWNER_PASSWORD
```

成功结果固定为一篇无图法律样稿、两套官方主题（高级极简、现代政务红）、2 次主题应用和
4 个“主题 × `standard` / `wechat_safe`”输出及 Copy Payload。结果只表示正式 Payload 已生成，
不表示真实 Safari 已写入剪贴板；仍须打开 `editorUrl` 点击复制并人工记录。

### 微信公众号含图验收

运行中的 API 与 seed 进程必须使用同一个公网 HTTPS `S3_PUBLIC_ENDPOINT`。Docker Compose
现在允许从宿主环境覆盖本地默认值；若使用本仓库 Compose，应先用该变量重建或重启 API：

```bash
export S3_PUBLIC_ENDPOINT="https://<公网对象存储地址>"
pnpm docker:dev

export ACCEPTANCE_OWNER_EMAIL="<现有 active Owner 邮箱>"
read -s "ACCEPTANCE_OWNER_PASSWORD?Owner password: "
export ACCEPTANCE_OWNER_PASSWORD
ACCEPTANCE_SCOPE=wechat pnpm acceptance:seed
unset ACCEPTANCE_OWNER_PASSWORD
```

若连接的是已经正确配置的远程环境，无需运行 `pnpm docker:dev`，但应设置对应的
`ACCEPTANCE_API_BASE_URL` 和 `ACCEPTANCE_WEB_BASE_URL`。远程地址只允许 HTTPS。

wechat scope 会在登录和任何写入前检查 Endpoint 为 HTTPS、DNS 仅解析到公网地址、当前执行机
可访问且不发生重定向或 5xx；随后上传或去重 52 张确定性 PNG，保存四篇固定样稿，应用两套
固定主题并生成 16 个正式输出。脚本还会确认 API 返回的签名 URL 与本次 Endpoint 一致，并从
当前执行机逐一 GET 52 个实际 Copy Payload 图片对象，要求 `2xx + image/*`。成功计数必须为：

```text
文章 = 4
图片 = 党政巡察 1 + 法律 0 + AI 科技 1 + 极端 50 = 52
resources.uploaded + resources.deduplicated = 52
themeApplications = 8
renderOutputs = 16
publicImageObjectsVerified = 52
copyRecordWritten = false
published = false
```

这些检查仍不能证明微信服务器自身可以抓取图片，也不能代替浏览器剪贴板、公众号后台粘贴、
手机预览和保存草稿。Copy Payload 有效期为 15 分钟，生成后应尽快人工验收。详细记录模板见
[`wechat-manual-test-record.md`](./wechat-manual-test-record.md)。

## V0.1 产品门槛

| 门槛 | 当前证据 | 状态 |
| --- | --- | --- |
| 用户登录 | Auth HTTP 测试、Docker 烟测、Playwright | **本轮 HTTP 6/6 通过；容器与浏览器待回归** |
| 新建文章 | Article HTTP 测试、Docker 烟测、Playwright | **本轮 HTTP 4/4 通过；容器与浏览器待回归** |
| 粘贴 Word / WPS 内容 | Import HTTP 测试、固定 Word Fixture、Docker 烟测 | **本轮 Import HTTP 6/6 通过；容器待回归** |
| 确认结构 | Import HTTP 测试、Docker 烟测 | **本轮 Import HTTP 通过；容器待回归** |
| 编辑排版与原文锁定 | Editor、Text-lock、Document HTTP、Playwright | **本轮单元与 HTTP 通过；浏览器待回归** |
| 图片资源引用一致性 | 保存/复制/导入/快照/恢复/正式渲染的 live/frozen 事务绑定；0003→0005 回填 | **实现完成，待全量与旧库迁移复验** |
| 2 套正式主题 | 官方不可变主题包、Theme HTTP、Renderer、Docker 烟测 | **本轮单元与 HTTP 通过；容器待回归** |
| 20–30 个正式组件 | 29 个官方组件及目录、插入、换色、渲染、快照和复制 | **本轮单元测试通过；浏览器待回归** |
| 异步任务中心 | PostgreSQL 权威记录、BullMQ、SSE、重试、取消、幂等、心跳 | **上一基线通过，本轮待回归** |
| 自动保存与历史恢复 | Autosave、Document、Snapshot、Playwright、Docker 烟测 | **本轮单元与 HTTP 通过；容器与浏览器待回归** |
| 微信预览与兼容定位 | Renderer、Compatibility、设备预览 Playwright | **本轮 Renderer 21/21 通过；浏览器待回归** |
| Chrome 复制 | Clipboard、Chromium E2E、Docker API 烟测 | **自动化待回归，真实后台待人工** |
| Safari 复制 | WebKit 仅验证内核流程；seed 可准备法律样稿 | **待真实 Safari 人工验收** |
| Edge 复制 | Chromium 覆盖共用标准能力分支 | **待真实 Edge 人工验收** |
| 微信公众号后台粘贴 | 四篇 / 52 图 seed 已实现 | **待真实后台人工验收** |
| P0 / P1 缺陷 | 本轮静态审计无 P0；迁移审计项已修复 | **待全量回归后确认** |
| Docker Compose 启动 | `pnpm docker:dev` | **上一基线通过，本轮待回归** |
| CI 通过 | GitHub Actions 三项作业 | **当前提交待推送后确认** |

## 发布判定

当前结论：**不可打 V0.1 正式标签**。

阻塞项：

1. 跑完本轮 Testcontainers、数据库迁移、Chromium / WebKit E2E 和 Docker 烟测；
2. 使用新建法律样稿完成真实 Safari 与 Edge 复制人工测试；
3. 以真实公网 HTTPS 对象存储成功执行 wechat seed，并完成四篇文章在公众号后台的粘贴、
   预览、移动端检查和保存草稿；
4. 推送当前提交并确认远端 CI 全部通过。

以上阻塞关闭后，确认 `P0 = 0`、`P1 = 0`，再创建 V0.1 标签。

## 公网生产上线的额外门槛

V0.1 功能标签不等于公网生产系统已经可部署。当前仓库的 Compose 和 Node 镜像仍以开发运行
方式为主；正式公网部署还需完成并演练：

1. 生产镜像、生产 Compose 或目标云平台运行清单，以及明确的发布/回滚流程；
2. 域名、TLS、反向代理、生产数据库/Redis/对象存储/SMTP 和全部 Secret 注入；
3. 上线前备份、维护窗口内迁移、恢复演练与回滚验证；`0004` 回填期间必须停止旧版本写入；
4. 日志、指标、告警、备份巡检和容量限制；
5. 在与生产等价的预发布环境重新执行本清单。
